{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter teams build",
  "installCommand": "echo 'install handled in buildCommand'",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
